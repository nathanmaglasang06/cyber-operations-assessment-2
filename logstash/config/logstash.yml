# =============================================================================
# Logstash Global Configuration - CarePlus Medical Group
# =============================================================================

http.host: "0.0.0.0"
http.port: 9600

# Pipeline settings
pipeline.workers: 2
pipeline.batch.size: 125
pipeline.batch.delay: 50

# Monitoring (disabled for single-node dev setup)
xpack.monitoring.enabled: false

# Logging
log.level: info
path.logs: /usr/share/logstash/logs
